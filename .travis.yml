sudo: required
language: go
go:
  - tip

services:
  - docker

before_install:
  - docker-compose -f docker-compose.yml up -d

#script:
#  - docker run todo_app_go go test -v -covermode=count -coverprofile=coverage.out

after_success:
  - docker login -u $DOCKER_ID -p $DOCKER_PASSWORD
  - docker push todo_app_go